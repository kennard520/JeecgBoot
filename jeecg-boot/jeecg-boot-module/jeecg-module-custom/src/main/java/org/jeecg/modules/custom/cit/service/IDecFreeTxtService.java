package org.jeecg.modules.custom.cit.service;

import com.baomidou.mybatisplus.extension.service.IService;
import org.jeecg.modules.custom.cit.entity.DecFreeTxt;

/**
 * 报关单自由文本信息DecFreeTxt Service。
 *
 * <p>封装 报关单自由文本信息DecFreeTxt 的业务操作入口，目前提供 MyBatis-Plus 标准服务能力，后续业务校验和报文装配逻辑应优先放在这里。</p>
 *
 * @author jeecg-boot
 * @since 3.9.1
 */
public interface IDecFreeTxtService extends IService<DecFreeTxt> {
}
